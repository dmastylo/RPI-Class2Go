DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql', # Add 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': '<%= @node[:aws][:database_instance] %>',                      # Or path to database file if using sqlite3.
        'USER': '<%= @node[:aws][:database_user] %>',                      # Not used with sqlite3.
        'PASSWORD': '<%= @node[:aws][:database_password] %>',                  # Not used with sqlite3.
        'HOST': '<%= @node[:aws][:database_host] %>',   # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
    }
}

# Set PRODUCTION to True so we don't show stackdumps on errors
PRODUCTION = <%= @node[:main][:production] %>

# Put your name and email address here, so Django serious errors can come to you
ADMINS = (
        ('<%= @node[:main][:admin_name] %>', '<%= @node[:main][:admin_email] %>')
        )

SECRET_KEY = '<%= @node[:main][:django_secret] %>'

AWS_ACCESS_KEY_ID = '<%= @node[:aws][:access_key] %>'
AWS_SECRET_ACCESS_KEY = '<%= @node[:aws][:access_secret] %>'
AWS_STORAGE_BUCKET_NAME = '<%= @node[:aws][:storage_bucket] %>'

MEDIA_ROOT = ''

PIAZZA_ENDPOINT = '<%= @node[:piazza][:endpoint] %>'
PIAZZA_KEY = '<%= @node[:piazza][:key] %>'
PIAZZA_SECRET = '<%= @node[:piazza][:secret] %>'

SES_SMTP_USER = '<%= @node[:aws][:ses_user] %>'
SES_SMTP_PASSWD = '<%= @node[:aws][:ses_passwd] %>'

